// Loop through numbers from 100 to 0 in descending order
for (var i = 100; i >= 0; i--) {
    // Check if the current number is divisible by 3
    if (i % 3 == 0) {
        console.log(i); // If true, print the current number
    }
}
