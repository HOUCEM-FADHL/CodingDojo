// Loop through numbers from 1 to 20
for (var i = 1; i <= 20; i++) {
    // Check if the current number is odd (has a remainder of 1 when divided by 2)
    if (i % 2 == 1) {
        console.log(i); // If true, print the current number
    }
}
