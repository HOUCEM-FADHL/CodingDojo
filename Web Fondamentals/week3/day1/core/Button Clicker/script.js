// Function to update the text content of an element to "Logout"
function logout(element) {
    element.innerText = "Logout";
}

// Function to remove an element from the DOM
function hide(element) {
    element.remove();
}
